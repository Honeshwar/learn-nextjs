import { Inter } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });
export const p = Inter({ subsets: ['vietnamese'],weight: ['400', '700'], });
export const lusitana = Inter({ subsets: ['vietnamese'] });